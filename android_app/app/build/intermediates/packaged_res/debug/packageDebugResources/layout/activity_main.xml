<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Media Player Fragment Container -->
    <FrameLayout
        android:id="@+id/mediaPlayerContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Sidebar Tools Container (Always on left side, matching web: left-4) -->
    <LinearLayout
        android:id="@+id/sidebarToolsContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="16dp"
        android:visibility="visible">

        <!-- Inquiry Button -->
        <ImageButton
            android:id="@+id/inquiryButton"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginBottom="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/inquiry"
            android:padding="12dp"
            android:src="@android:drawable/ic_menu_help"
            android:tint="#FFFFFF" />

        <!-- Microphone Button -->
        <ImageButton
            android:id="@+id/microphoneButton"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginBottom="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="الميكروفون"
            android:padding="12dp"
            android:src="@android:drawable/ic_btn_speak_now"
            android:tint="#FFFFFF"
            android:visibility="gone" />

        <!-- Audio Control Button (Music) -->
        <ImageButton
            android:id="@+id/audioControlButton"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="الموسيقى"
            android:padding="12dp"
            android:src="@android:drawable/ic_media_play"
            android:tint="#FFFFFF"
            android:visibility="gone" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
